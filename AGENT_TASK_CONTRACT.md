# Agent 任务合同模板

用于给任意 Agent 下达可执行、可验收、可追踪的任务。

---

## 1) 基础信息

- 合同编号：
- 日期：
- 发包方（你）：
- 承包 Agent 角色：`PM/架构` | `前端` | `后端` | `QA`
- 优先级：`P0` | `P1` | `P2`
- 截止时间（Deadline）：

---

## 2) 任务目标（必须量化）

- 业务目标：
- 技术目标：
- 成功标准（可验证）：
  - [ ] 标准 1
  - [ ] 标准 2
  - [ ] 标准 3

---

## 3) 范围定义（防止跑偏）

### In Scope（必须做）
- 
- 

### Out of Scope（明确不做）
- 
- 

---

## 4) 输入材料（你提供给 Agent）

- 需求文档：
- 设计稿/Figma：
- 相关代码路径：
- 接口文档/数据库信息：
- 依赖方联系人（如前后端对接人）：

---

## 5) 交付物要求（Agent 必交）

- 代码变更：
- 文档更新：
- 测试结果：
- 风险说明：
- 下一步建议：

---

## 6) 验收标准（Acceptance Criteria）

- [ ] 功能符合需求且可复现演示
- [ ] 不破坏现有核心流程（回归通过）
- [ ] 关键边界条件有处理（空态/错误态/超时等）
- [ ] 无新增高优先级 lint/type 错误
- [ ] 变更说明清晰，可被他人接手

---

## 7) 沟通与汇报节奏

- 日常同步：每天/每里程碑一次
- 风险升级规则：遇到阻塞超过 `X` 小时必须升级
- 固定汇报格式：
  - 进度：已完成 / 进行中 / 未开始
  - 阻塞：是什么、影响、需要谁决策
  - 下一步：下一阶段明确动作

---

## 8) 角色专用补充条款（按需选）

### A. PM/架构 Agent
- 必须输出任务拆解、依赖关系、里程碑。
- 必须明确前后端接口边界和验收口径。

### B. 前端 Agent
- 必须覆盖加载态、空态、错误态。
- 必须说明组件复用与可访问性处理。

### C. 后端 Agent
- 必须说明鉴权、校验、错误码、日志策略。
- 涉及数据变更时必须附迁移/回滚方案。

### D. QA Agent
- 必须给出测试用例清单（核心流程 + 边界情况）。
- 必须给出缺陷等级（高/中/低）和复现步骤。

---

## 9) 结项记录（验收时填写）

- 实际完成时间：
- 验收结论：`通过` | `有条件通过` | `不通过`
- 遗留问题：
- 后续行动项：

---

## 10) 一键下达示例（可复制）

```text
你是【后端 Agent】。请按以下任务合同执行：

【目标】
在 2 天内完成订单查询 API（支持分页、状态筛选），并保证现有下单流程不受影响。

【范围】
In Scope: 新增 GET /api/orders；增加 status、page、pageSize 参数；返回总数。
Out of Scope: 不改支付流程，不做导出功能。

【输入】
代码路径：src/app/api, src/lib/data.ts
依赖：前端将按你定义的返回结构接入

【交付】
1) 代码 + 必要注释
2) 接口说明（请求/响应示例）
3) 测试结果
4) 风险与回滚建议

【验收】
- 可按状态筛选并分页
- 无新增高优先级错误
- 返回结构与文档一致
```

